/**
 * Menu choice
 */
export class Choice {
  /**
   * Key for choice. Used for suffix / actual i18n key.
   */
  key: string;

  /**
   * Interaction to execute for this choice.
   */
  interaction: string;
}
